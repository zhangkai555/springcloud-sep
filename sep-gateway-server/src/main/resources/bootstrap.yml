spring:
  cloud:
    config:
      name: gateway
      label: test
      discovery:
        enabled: true
        serviceId: sep-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8888/eureka/
