spring:
  cloud:
    config:
      name: order,feign,database,redis,mybatis-plus
      label: test
      discovery:
        enabled: true
        serviceId: sep-config-server
eureka:
  client:
    serviceUrl:
      defaultZone: http://127.0.0.1:8888/eureka/

